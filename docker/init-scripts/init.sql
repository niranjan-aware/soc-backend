-- Create database extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Create tables will be handled by Hibernate, but we can add indexes here later
-- This file can be used for initial data seeding if needed
